// src/components/dashboard/PerformanceChart.jsx

import React from 'react';
import Card from '../ui/Card';

// Por ahora, este es solo un placeholder para que no haya errores.
// Más adelante instalaremos 'recharts' para hacer el gráfico real.
const PerformanceChart = ({ data }) => {
  return (
    <div className="w-full h-64 bg-gray-100 flex items-center justify-center rounded-lg">
      <p className="text-gray-500">Gráfico de rendimiento en construcción.</p>
    </div>
  );
};

export default PerformanceChart;